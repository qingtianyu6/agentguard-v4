services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: agentguard
      POSTGRES_PASSWORD: agentguard
      POSTGRES_DB: agentguard
    ports: ["5432:5432"]
    volumes: ["agentguard_pg:/var/lib/postgresql/data"]
volumes:
  agentguard_pg:
